import { createAssistantCanvasWorkflowSkills } from "../../assistantCanvasWorkflowSkills.js";

export function createWorkflowAdapter(options = {}) {
  return createAssistantCanvasWorkflowSkills(options);
}
