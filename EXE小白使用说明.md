# 幻映工作台 EXE 使用说明

这是一份给普通用户看的说明，不需要懂代码。

## 1. 怎么启动

1. 找到 `huanying-workbench-...-windows-x64-onefile.exe`
2. 直接双击这个 `exe`
3. 稍等几秒，浏览器会自动打开工作台页面

如果浏览器没有自动打开，可以手动访问：

`http://127.0.0.1:8777/`

## 2. 第一次启动为什么会慢一点

第一次运行时，单文件 `exe` 会先解压运行需要的内容，然后再启动本地服务，这是正常现象。

一般等几秒到十几秒都算正常。

## 3. 怎么关闭

有两种方式：

1. 直接关闭启动后出现的命令窗口
2. 先关浏览器，再关命令窗口

只关浏览器页面，不一定会把程序一起关掉。

## 4. 我的数据保存在哪里

默认保存在这里：

`%LOCALAPPDATA%\AI-CanvasPro`

里面会保存你的项目数据、输出内容和一些本地配置。

## 5. 如果双击后打不开怎么办

按下面顺序试：

1. 先等 5 到 15 秒
2. 手动打开浏览器访问 `http://127.0.0.1:8777/`
3. 关闭程序后重新双击一次
4. 换一个简单路径保存 `exe`，比如桌面或 `D:\Huanying`

## 6. 使用时的小提醒

- 建议把 `exe` 放在固定文件夹里使用
- 第一次运行如果 Windows 弹出安全提示，选择允许本地运行
- 杀毒软件如果拦截，请先把程序加入信任
- 不建议放在中文过多、层级太深、权限太严格的目录里

## 7. 怎么把它发给别人

把打出来的那个 `.exe` 文件发给对方就可以。

对方拿到后：

1. 放到自己电脑的一个固定文件夹
2. 双击运行
3. 等浏览器自动打开
