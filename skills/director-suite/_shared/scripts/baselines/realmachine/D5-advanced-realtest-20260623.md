# D5 进阶真机（含人物 + 主体分割 + 头身比）· 2026-06-23

> 续 D5-realtest（满帧纯色）。本次出**含人物全身像**,纯 Pillow 灰键抠主体后测**人物本身**的主色与头身比,
> 看 D5 在"真实人物图"上的有效边界。**诚实结论:主体色一致性跑通;头身比像素启发式不可靠,需姿态关键点。**

## 出图（grsai nano-banana-2，全身像 9:16，纯灰底 #888888）

| 图 | 内容 | 字节 |
|---|---|---|
| A_real8 | 藏青唐装中年男·realistic ~8头身 | 1.14MB |
| B_real8b | 同 A 重生（一致性） | 0.41MB |
| C_chibi | 同人·chibi Q版 ~2.5头身（头身漂移） | 0.81MB |

原图存 `D:\Backup\d5b-realtest-20260623\`，未入仓。

## 测量（纯 Pillow 灰键抠图:去掉近 #888888 低彩像素 = 背景,余为主体）

```
A_real8  主色 hue 225.8° / L 18.2%   头身比(启发式) 5.6   主体像素18027 总高285 头高51
B_real8b 主色 hue 232.0° / L 21.9%   头身比 5.5            （与A几乎逐位复现）
C_chibi  主色 hue 10.3°  / L 35.9%   头身比 8.0
```

## D5 审计

```
B重生 vs A → GO    头身比偏移 1.8% / 色相 6.2° / 明度 3.7pp  全过   （同人重生,一致性保持）
C-Q版 vs A → NO-GO 头身比 42.9% FAIL / 色相 144.5° FAIL / 明度 17.7pp FAIL
```

## 两个真发现（真机测出、结构秤测不出）

1. ✅ **主体分割 + 主色一致性 = 真跑通、可复现**:抠掉灰底测**人物本身**主色(不被背景稀释),A vs B 色相 6.2°、且头身比 5.6↔5.5 **逐位级复现**——说明灰键分割+测色是**精确**的。比 D5-realtest 的"满帧纯色"实质进一步:现在能测**带背景的人物图**的角色色彩一致性。
2. ⚠️ **头身比像素启发式不可靠(本次最重要的诚实发现)**:
   - 真·8头身被测成 **5.6**(系统性偏小);Q版(应 ~2.5)被测成 **8.0**(**方向反了**)。
   - 原因:用"肩部突宽(>1.7×头宽)"定头底,在非常规身材(大头小身)上崩溃。
   - **结论:轮廓像素给不出可信头身比。需真姿态关键点(mediapipe/openpose 检测头顶/肩/脚)才能测。** 这**证伪了 D5-PRD「像素对图测头身比」的默认假设**。
   - C 被正确判 NO-GO,但**可信依据是色相**(Q版那张模型把藏青渲成偏红 hue10.3°),头身那条数字不作数。

## 固化策略（诚实,不固化不可信项）

- ✅ 固化:主体色一致性已由 D5-realtest 的 `consistency_real` 反向对覆盖;本次再证它在带背景人物图上成立。
- ❌ **不固化头身比作"能用"检查**:像素启发式不可信。`audit_consistency` 的 `headbody_ratio` 比对仅适用于**已由可信工具(关键点)测得的数值**,不适用于像素轮廓估算——已在本文件钉死此边界。下一步真做头身比需引入姿态关键点模型(超出本批纯标准库+小依赖范围)。
