# D5 真机像素验证（2026-06-23）· 评审整改证据

> 回应多角度评审最关键的一条:「D5 从没对真图测过一次,clean case 是 candidate==baseline 偏移恒 0,
> 像素对图是死路径」。本次用真机出图 + Pillow 实测,**让 D5 的像素路径第一次在真实生成图上跑通**,
> 且证明阈值能真区分"同色重生"与"换色漂移"。数字是真测的,不是手写。

## 出图（grsai nano-banana-2，付费由用户授权）

| 图 | prompt（满帧纯色,便于可控测色相） | 字节 | grsai url（可能过期） |
|---|---|---|---|
| A 基准 | deep navy blue silk #1C2A3A full frame | 1.97MB | file3.aitohumanize.com/.../d66152ef…png |
| B 重生 | 同 A（一致性测试） | 1.74MB | （同上批次） |
| C 漂移 | deep wine-red silk #7A1F2B full frame | 1.17MB | （同上批次） |

原图存 `D:\Backup\d5-realtest-20260623\{A_navy,B_navy2,C_winered}.png`（未入仓:二进制大文件 + 本仓 git 对象损坏）。

## 实测（`audit_consistency._measure_from_image`，Pillow 12.2.0）

```
A 基准(藏青) = hue 216.8° / lightness 17.9%
B 重生(藏青) = hue 216.5° / lightness 12.0%
C 漂移(酒红) = hue 353.3° / lightness 15.6%
```

## D5 真图审计结论

```
B(藏青重生) vs A  → GO    色相偏移 0.3° ≤15° PASS  明度偏移 5.9pp ≤10 PASS   （模型同色重生,正确放行）
C(酒红)     vs A  → NO-GO 色相偏移 136.5° >15° FAIL                          （真实换色漂移,正确抓住）
```

## 结论与诚实边界

- ✅ D5 的**像素测量+阈值在真实负载上被标定了一次**:0.3°(同色)放行 / 136.5°(换色)报红,15° 阈值有判别力。
- ✅ 顺带发现:同 prompt 重生的**明度**仍有 5.9pp 自然波动(17.9→12.0),仍在 10pp 阈值内——阈值合理但不宽松。
- ⚠️ 仍**只测了「满帧纯色」的主色一致性**(色相/明度)。**头身比/LOGO坐标/脸部结构**需关键点检测,本次未测;含背景/人物的真实分镜出图,主色会被背景稀释,需先做主体分割再测——这是 D5 的下一步真机标定项。
- 配套:`fixtures/D5_consistency_real.{clean,poison}.json` 用**本次真测数字**作 D5 的非自指反向对(register 名 `consistency_real`)。
