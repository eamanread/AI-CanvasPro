"""director-suite 共享工程底座（harness）。纯机制、零业务、仅标准库。

见 docs/optimization/dev/00-ENGINEERING-SUBSTRATE.md。
构件：audit_report / lexicon+leak_scan / portability_scan / reverse_test / run_eval(+registry) / regression(L2回归门:审计被弱化即FAIL)。
"""
